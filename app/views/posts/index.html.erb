<h1>Welcome Users</h1>
<p>Lets Play the Trivia Game</p>
<% @posts.each do |post| %>
  <div class="list-group-item">
    <h3>Q. <%= post.question %></h3>
    <p><%= post.points %></p>
    <%= link_to "Play", post_path(post), :class => 'btn btn-default' %>
  </div>
<% end %>